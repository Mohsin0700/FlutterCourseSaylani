void main() {
  // In this class we'll learn git & github
  // First of all we will download git from it's official website for our opertaing system

  // Then we will signup on github

  // Then we will use following commands using gitbash, cmd or vs_code's terminal

  // git config --global user.name = 'user name'(For once)
  // git config --global user.email = 'email'(For once)

  // Now we will create a new repositery on github

  // Now in terminal we'll go to main folderof our project and in terminal we will write "git init". It will initialize git in our project.

  // Now we will connect our local folder with our main repositery on github by using command "git remote add origin "our repo link which will be showing in a new repo"(For once)"

  // Now "git add ." this command will add all the files/folders of projects to be uploaded on github

  // Or we can write "git add filename" to add any specific file / folder to upload on github

  // Then we write "git commit -m 'Comment/ message' "

  // Finanlly git push -u origin master

  // Congratlaions our files/folder has been uploaded successfully

  // "git status" command is used to check files / project status

  // FOR UPDATION OF PROJECT
  // git add . or git add filename
  // git commit -m 'message'
  // git push
}
