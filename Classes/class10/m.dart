import 'dart:io';

void main() {
  // // Pyramid Using Sir Bilal's method
  // int count = 5;
  // var spc = ' ';

  // // for (var i = 1; i < count; i++) {
  // //   stdout.write('${spc * (count - i)}');
  // //   for (var j = 1; j <= i; j++) {
  // //     stdout.write("*$spc");
  // //   }
  // //   print('');
  // // }

  // // Pyramid Using My Own logic

  // for (var i = 0; i < count; i++) {
  //   stdout.write(spc * (count - i));
  //   stdout.write('* ' * i);
  //   print('');
  // }
  // Alhamdulillah Done with own logic
  // Now I will create a todo app using pure dart in project folder of the same repo....
}
